// Objeto em javaScript é dinamico então é possivel criar atributos e metodos de forma dinamica
const produto = {}; // Declaração de um objeto 
produto.nome = "Coca Cola"; // Criando um atributo do objeto
produto.preco = 4;

console.log(produto);

// É possivel tambem criar o objeto e os atributos juntos parecido com java 
const pessoa = {
    nome:"Wesley",
    idade: 23
}


console.log(pessoa);