// function declaration

function soma(x, y) {
    return x + y;
}

// funcion expession
const sub = function () {
    return x - y;
}


// named funcion expression
const mult = function mult(x, y) {
    return x * y;
}